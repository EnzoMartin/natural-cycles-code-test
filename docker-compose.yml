version: "3"
services:

  mysql:
    image: mysql:8
    environment:
      - MYSQL_DATABASE="natural-cycles"
    volumes:
       - /data/mysql:/var/lib/mysql
    networks:
      - db
    env_file:
      - ./.env

  certbot:
    image: certbot/certbot:latest
    command: certbot --nginx certonly
    volumes:
      - /data/certbot/letsencrypt/:/var/www/letsencrypt
      - /data/certbot/certs/:/var/certs
      - /data/letsencrypt/etc/:/etc/letsencrypt
      - /data/letsencrypt/var/:/var/lib/letsencrypt
    links:
      - nginx
    networks:
      - server

  nginx:
    image: enzomartin78/natural-cycles-code-test-nginx:master-latest
    volumes:
      - /data/nginx/logs/:/var/log/nginx
      - /data/certbot/letsencrypt/:/var/www/letsencrypt
      - /data/certbot/certs/:/var/certs
      - /data/letsencrypt/etc/:/etc/letsencrypt
      - /data/letsencrypt/var/:/var/lib/letsencrypt
    ports:
      - "80:80"
      - "443:443"
    links:
      - service
    networks:
      - server

  service:
    image: enzomartin78/natural-cycles-code-test:master-latest
    environment:
      - NODE_ENV="production"
    restart: unless-stopped
    depends_on:
      - mysql
    links:
      - mysql
    networks:
      - db
      - server
    env_file:
      - ./.env

networks:
  server:
  db:
